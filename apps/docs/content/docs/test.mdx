---
title: Animated Switch
description: A precise switch, engineered with motion physics. Beauty in every
  pixel. Seamless by default. Use the live controls below to explore
  controlled/uncontrolled modes, stress tests, and why extra
  className that alters sizing will break the switch's internal
  geometry.
component: true
motion: true
features:
  - title: Built-in spring motion
    icon: Play
  - title: Fine-tuned physics (stiffness:500)
    icon: Zap
  - title: Pixel-perfect handle movement
    icon: Eye
---

# Animated Switch — Showcase

Demo lengkap komponen **Animated Switch**: hero visual, feature cards, instalasi, contoh live, anatomy, props, dan resources terkait — semuanya dirender dalam layout bento-grid untuk tampilan ala landing page SaaS.

  <DocGridSection type="hero" span="full">
    <section className="relative rounded-xl overflow-hidden shadow-lg">
      {/* hero background (video iframe) */}
      <div className="aspect-16/6 w-full relative bg-neutral-900/60">
        <iframe
          className="absolute inset-0 w-full h-full pointer-events-none"
          src="https://www.youtube.com/embed/ysz5S6PUM-U?autoplay=1&mute=1&loop=1&playlist=ysz5S6PUM-U"
          title="Hero demo loop"
          frameBorder="0"
          allow="autoplay; encrypted-media"
        />
        <div className="absolute inset-0 bg-linear-to-b from-transparent to-black/60" />
      </div>

      <div className="p-8 md:p-12 relative bg-linear-to-t from-neutral-900/80 to-transparent text-white">
        <div className="max-w-4xl">
          <h1 className="text-4xl md:text-5xl font-extrabold leading-tight">
            Animated Switch — precise motion, effortless integration
          </h1>
          <span className="mt-4 text-lg max-w-2xl">
            A tiny control, engineered for delight. Built with spring physics,
            accessibility-ready, and built to feel natural across platforms.
            Drop it into your UI and watch it breathe.
          </span>

          <div className="mt-6 flex flex-wrap gap-3">
            <a
              className="inline-flex items-center gap-2 rounded-lg px-4 py-2 bg-white/10 hover:bg-white/12 border border-white/10"
              href="#try-live"
            >
              Try live
            </a>
            <a
              className="inline-flex items-center gap-2 rounded-lg px-4 py-2 bg-primary/90 text-white"
              href="#installation"
            >
              Install
            </a>
          </div>

          <div className="mt-8 flex gap-4 items-center">
            <div className="w-28 h-14 rounded-lg bg-white/10 flex items-center justify-center">
              {/* Small live preview frame (static fallback) */}
              <div className="text-sm">On / Off</div>
            </div>

            <div className="text-sm text-neutral-300">
              Lightweight — ~1.8kb gzipped. Works with React or vanilla JS.
            </div>
          </div>
        </div>
      </div>
    </section>

  </DocGridSection>

  <DocGridSection type="features" span="half">
    <div className="space-y-4">
      <h3 className="font-semibold text-lg">Why it matters</h3>

      <ul className="space-y-3">
        <li className="flex items-start gap-3">
          <Play className="h-5 w-5 shrink-0 mt-1" />
          <div>
            <div className="font-medium">Built-in spring motion</div>
            <div className="text-sm text-neutral-500">Polished, natural movement.</div>
          </div>
        </li>

        <li className="flex items-start gap-3">
          <Zap className="h-5 w-5 shrink-0 mt-1" />
          <div>
            <div className="font-medium">Fine-tuned physics</div>
            <div className="text-sm text-neutral-500">Stiffness tuned to 500 for crisp response.</div>
          </div>
        </li>

        <li className="flex items-start gap-3">
          <Eye className="h-5 w-5 shrink-0 mt-1" />
          <div>
            <div className="font-medium">Pixel-perfect handle movement</div>
            <div className="text-sm text-neutral-500">Consistent visuals even under transforms.</div>
          </div>
        </li>
      </ul>
    </div>

  </DocGridSection>

  <DocGridSection type="installation" span="half">
    <h3 className="font-semibold text-lg">Install</h3>

    <CodeBlock language="bash" code={`npm install @your-org/animated-switch

# or

yarn add @your-org/animated-switch`} />

    <span className="mt-3 text-sm text-neutral-500">
      Works with React 18+. Peer dependencies: framer-motion (optional) — built to gracefully degrade.
    </span>

  </DocGridSection>

  <DocGridSection type="example" span="third">
    <h4 className="font-semibold">Live playground</h4>

    <div id="try-live" className="mt-3">

        <div className="flex flex-col gap-4 items-start">
          <div className="rounded p-4 border w-full max-w-xs">
            <strong>Controlled</strong>
            <div className="mt-3">
              <div className="inline-flex items-center gap-3">
                <div className="rounded-full w-12 h-6 bg-neutral-200/30 flex items-center p-0.5">
                  <div className="bg-white w-5 h-5 rounded-full shadow" />
                </div>
                <div className="text-sm text-neutral-500">Off</div>
              </div>
            </div>
          </div>

          <div className="rounded p-4 border w-full max-w-xs">
            <strong>Uncontrolled</strong>
            <div className="mt-3 text-sm text-neutral-500">Default internal state, keyboard accessible.</div>
          </div>
        </div>
    </div>

  </DocGridSection>

  <DocGridSection type="example" span="third">
    <h4 className="font-semibold">Usage (JSX)</h4>

    <CodeBlock language="tsx" code={`import { AnimatedSwitch } from "@your-org/animated-switch";

function Example() {
const [on, setOn] = useState(false);

return (

<AnimatedSwitch
  checked={on}
  onChange={setOn}
  aria-label="Enable notifications"
/>
); }`} />

  </DocGridSection>

  <DocGridSection type="example" span="third">
    <h4 className="font-semibold">Accessibility</h4>

    <ul className="text-sm space-y-2">
      <li><strong>Keyboard:</strong> toggle with <code>Space</code></li>
      <li><strong>ARIA:</strong> exposes <code>role="switch"</code> and <code>aria-checked</code></li>
      <li><strong>Focus:</strong> visible ring and focus management</li>
    </ul>

  </DocGridSection>

<DocGridSection type="warning">
  <strong>Note:</strong> If you override sizing via an external{" "}
  <code>className</code> that scales the root, the internal handle geometry may
  shift. Prefer CSS variables (see <code>--switch-width</code>,{" "}
  <code>--handle-size</code>) for safe customization.
</DocGridSection>

  <DocGridSection type="anatomy" span="full">
    <h3 className="font-semibold">Anatomy</h3>

    <svg viewBox="0 0 900 220" className="w-full h-56 mt-4 rounded-lg border">
      <rect x="10" y="10" width="880" height="200" rx="12" fill="#0f172a" />
      <g transform="translate(40,40)">
        <rect x="0" y="0" width="220" height="80" rx="10" fill="#111827" />
        <text x="14" y="26" fill="#E6E6E6" fontSize="14">Switch root</text>

        <rect x="260" y="0" width="220" height="80" rx="10" fill="#111827" />
        <text x="274" y="26" fill="#E6E6E6" fontSize="14">Track</text>

        <rect x="520" y="0" width="220" height="80" rx="10" fill="#111827" />
        <text x="534" y="26" fill="#E6E6E6" fontSize="14">Handle</text>
      </g>
    </svg>

    <span className="mt-4 text-sm text-neutral-500">
      Root manages focus & keyboard; track visually represents the state; handle is animated independently.
      Use CSS variables to control sizes and transition durations.
    </span>

  </DocGridSection>

  <DocGridSection type="props" span="full">
    <h3 className="font-semibold">Props</h3>

    <table className="w-full text-sm mt-4">
      <thead>
        <tr className="text-left">
          <th>Name</th>
          <th>Type</th>
          <th>Default</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>checked</code></td>
          <td><code>boolean</code></td>
          <td><code>false</code></td>
          <td>Controlled checked state.</td>
        </tr>
        <tr>
          <td><code>defaultChecked</code></td>
          <td><code>boolean</code></td>
          <td><code>false</code></td>
          <td>Uncontrolled initial state.</td>
        </tr>
        <tr>
          <td><code>onChange</code></td>
          <td><code>(v: boolean) =&gt; void</code></td>
          <td>—</td>
          <td>Called when toggle changes.</td>
        </tr>
        <tr>
          <td><code>className</code></td>
          <td><code>string</code></td>
          <td>—</td>
          <td>Additional classes for root element. Prefer CSS variables to resize.</td>
        </tr>
      </tbody>
    </table>

  </DocGridSection>

  <DocGridSection type="related" span="half">
    <h4 className="font-semibold">Related</h4>

    <ul className="mt-3 space-y-2">
      <li>
        <a className="flex items-center gap-2" href="/docs/switch-advanced">
          <GitBranch className="h-4 w-4" /> Advanced Switch patterns
        </a>
      </li>
      <li>
        <a className="flex items-center gap-2" href="/docs/motion-guidelines">
          <Video className="h-4 w-4" /> Motion guidelines
        </a>
      </li>
      <li>
        <a className="flex items-center gap-2" href="/docs/accessibility">
          <Image className="h-4 w-4" /> Accessibility checklist
        </a>
      </li>
    </ul>

  </DocGridSection>

  <DocGridSection type="example" span="two-thirds">
    <h4 className="font-semibold">Full example — Combining with form</h4>

    <CodeBlock language="tsx" code={`function NotificationRow() {

const [enabled, setEnabled] = useState(true);
return (

<div className="flex items-center justify-between gap-4">
  <div>
    <div className="font-medium">Email notifications</div>
    <div className="text-sm text-neutral-500">
      Receive weekly product updates
    </div>
  </div>
  <AnimatedSwitch checked={enabled} onChange={setEnabled} />
</div>
); }`} />

    <div className="mt-6">
      <img
        alt="preview"
        src="https://images.unsplash.com/photo-1529333166437-7750a6dd5a70?q=80&w=1200&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder"
        className="w-full rounded-lg border"
      />
    </div>

  </DocGridSection>

  <DocGridSection type="example" span="third">
    <h4 className="font-semibold">Performance</h4>

    <span className="text-sm text-neutral-500">
      Runs at 60fps on modern devices. Uses <code>transform</code> and <code>will-change</code>
      for efficient compositing. Minimal reflow on state changes.
    </span>

  </DocGridSection>

  <DocGridSection type="example" span="full">
    <h4 className="font-semibold">Canvas sketch — motion preview</h4>

    <canvas
      id="switch-motion-canvas"
      width="900"
      height="240"
      className="w-full h-60 rounded-lg border bg-white/5"
    >
      {/* Fallback content for non-canvas environments */}
      Your browser does not support canvas.
    </canvas>

    <span className="mt-3 text-sm text-neutral-500">
      (Canvas is rendered by the docs runtime — it draws a trajectory preview of the handle motion.)
    </span>

  </DocGridSection>

---

## Notes for implementor

- Replace preview placeholders (PreviewBlock, canvas mounting) with your docs runtime code so examples become interactive.
- Use the `span` props on `<DocGridSection>` to arrange cards — we've used `full`, `half`, `third`, and `two-thirds` to compose a landing-like layout.
- Images: use optimized CDN images in production.

---
