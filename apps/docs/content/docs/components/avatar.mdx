---
title: Avatar
description: An image element used to represent a user or entity, typically displayed as a circular or square thumbnail. Can fall back to initials or an icon when no image is provided.
component: true
---

import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { AvatarBasic, InitialAvatar, ShapeAvatar, SizeAvatar, AvatarGroup } from "@/components/docs/avatar"

## Basic

<AvatarBasic />

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
<Tab value="Manual">

Install following dependencies:

```package-install
npm install class-variance-authority clsx twMerge
```

Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

```ts ts2js title="lib/utils.ts"
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Copy and paste the following code into your project.

```tsx ts2js title="components/ui/avatar"
"use client";

import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/lib/utils";

const avatarVariants = cva(
  "-outline-offset-1 inline-grid shrink-0 align-middle outline-1 outline-foreground/(--ring-opacity) [--avatar-radius:20%] [--ring-opacity:20%] *:col-start-1 *:row-start-1",
  {
    variants: {
      size: {
        xs: "size-6",
        sm: "size-8",
        md: "size-10",
        lg: "size-12",
        xl: "size-16",
        "2xl": "size-20",
      },
      variant: {
        circle: "rounded-full *:rounded-full",
        square: "rounded-(--avatar-radius) *:rounded-(--avatar-radius)",
      },
    },
    defaultVariants: {
      size: "md",
      variant: "circle",
    },
  }
);

interface AvatarProps
  extends React.ComponentProps<"span">,
    VariantProps<typeof avatarVariants> {
  src?: string | null;
  initials?: string;
  alt?: string;
}

function Avatar({
  src = null,
  initials,
  alt = "",
  className,
  variant,
  size,
  ...props
}: AvatarProps) {
  return (
    <span
      data-slot="avatar"
      {...props}
      className={cn(avatarVariants({ size, variant }), className)}
    >
      {initials && (
        <svg
          className="size-full select-none fill-current p-[5%] font-md text-[48px] uppercase bg-background"
          viewBox="0 0 100 100"
          aria-hidden={alt ? undefined : "true"}
        >
          {alt && <title>{alt}</title>}
          <text
            x="50%"
            y="50%"
            alignmentBaseline="middle"
            dominantBaseline="middle"
            textAnchor="middle"
            dy=".125em"
          >
            {initials}
          </text>
        </svg>
      )}

      {/* Use Image from next/image if you're in NextJS */}
      {src && (
        <img
          className="size-full object-cover object-center"
          src={src}
          alt={alt}
        />
      )}
    </span>
  );
}

export type { AvatarProps };
export { Avatar, avatarVariants };
```

</Tab>
<Tab value="CLI">

```package-install
npx libravelui@latest add avatar
```

</Tab>
</Tabs>

## Anatomy

```tsx
import { Avatar } from "@/components/ui/avatar";
```

```tsx
<Avatar src="https://i.pravatar.cc/100?img=3" alt="Dominic Torpedo" />
```

## Fallback

Displays an avatar using user initials when no profile image is available. This ensures consistent styling and fallback behavior while still giving a personalized touch with capitalized initials.

<InitialAvatar />

## Shapes

Showcases different avatar shapes: **circle** and **square**. The shape can be controlled via the `variant` prop, allowing you to match the avatar’s appearance with your app’s design language.

<ShapeAvatar />

## Sizes

Demonstrates the avatar in various sizes, from **extra small (xs)** to **extra large (xl)**. The `size` prop offers flexibility for using the avatar in different UI contexts—such as user lists, comments, or main profile views.

<SizeAvatar />

## Avatar Group

Displays a group of avatars representing multiple users. Each avatar can show either a profile image or fallback initials if the image is not available. Avatar groups are commonly used in collaborative interfaces, team listings, or messaging apps.

<AvatarGroup />

## Props

<TypeTable
  type={{
    src: {
      description: "The source URL of the avatar image. If not provided, initials will be shown instead (if set).",
      type: "string | null",
      required: false,
    },
    initials: {
      description: "Initials to display when no image source is provided.",
      type: "string",
      required: false,
    },
    alt: {
      description: "Alternative text for the image, used for accessibility.",
      type: "string",
      required: false,
    },
    size: {
      description:
        'Size of the avatar. Options: `"xs"`, `"sm"`, `"md"`, `"lg"`, `"xl"`, `"2xl"`.',
      type: `"xs" | "sm" | "md" | "lg" | "xl" | "2xl"`,
      required: false,
    },
    variant: {
      description:
        'Shape of the avatar. Options: `"circle"` for fully rounded, or `"square"` for slightly rounded corners.',
      type: `"circle" | "square"`,
      required: false,
    },
    className: {
      description: "Additional CSS classes to apply to the root element.",
      type: "string",
      required: false,
    },
    children: {
      description: "Elements passed as children. Generally not needed for this component.",
      type: "React.ReactNode",
      required: false,
    },
  }}
/>
