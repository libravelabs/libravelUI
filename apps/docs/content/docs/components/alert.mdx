---
title: Alert
description: A styled text component for highlighting important information, warnings, or contextual messages within a UI.
component: true
---

## Basic

<Playground comp="alert/basic" />

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
  <Tab value="Manual">
    <RequiredBlock components="field" />
    Install following dependencies:

    ```package-install
    npm install react-aria-components tw-animate-css tailwindcss-react-aria-components clsx tailwind-merge motion
    ```

    Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

    <ComponentSource comp="lib/utils" isReact={false} />

    Copy and paste the following code into your project.

    <ComponentSource comp="components/ui/alert" />

  </Tab>
  <Tab value="CLI">
    ```package-install
    npx libravelui@latest add alert
    ```
  </Tab>
</Tabs>

## Anatomy

```tsx
import { Alert } from "@/components/ui/alert";
```

```tsx
<div className="space-y-4">
  <Alert
    title="Default Alert"
    message="Alert for default"
    icon={<Bell className="size-5" data-slot="icon" />}
  />
  <Alert
    variant="info"
    title="Info Alert"
    message="Alert for info"
    icon={<Info className="size-5" data-slot="icon" />}
  />
  <Alert
    variant="success"
    title="Success Alert"
    message="Alert for success"
    icon={<CheckCircle className="size-5" data-slot="icon" />}
  />
  <Alert
    variant="warning"
    title="Warning Alert"
    message="Alert for warning"
    icon={<AlertTriangle className="size-5" data-slot="icon" />}
  />
  <Alert
    variant="destructive"
    title="Error Alert"
    message="Alert for destructive"
    icon={<XCircle className="size-5" data-slot="icon" />}
  />
</div>
```

## Examples

### Variants

The **Alert** component provides several variants to display different types of important messages: neutral, info, success, warning, and error.

<Playground comp="alert/variants" />

### Custom Build

The **Custom Alert** is built using low-level primitives like `AlertRoot`, `AlertTitle`, and `AlertDescription`. This setup offers full control over structure and behavior, making it easy to integrate buttons, icons, or custom interactions. It also includes a close action, allowing users to dismiss the alert manually when appropriate.

<Playground comp="alert/custom-build" />

### Can be Closed

An alert that can be manually dismissed by the user. This pattern improves usability by allowing temporary messages to be closed once they’re no longer needed, keeping the interface clean and focused.

<Playground comp="alert/can-closed" />

### Controlled State

An alert that can be manually dismissed by the user. This pattern improves usability by allowing temporary messages to be closed once they’re no longer needed, keeping the interface clean and focused.

<Playground comp="alert/controlled" />

## Props

### Alert

<TypeTable
  type={{
    variant: {
      description: "Defines the visual style of the alert",
      type: '"default" | "destructive" | "warning" | "info" | "success"',
      default: '"default"',
    },
    title: {
      description: "The title text displayed in the alert (optional)",
      type: "string",
    },
    message: {
      description: "The description or body text of the alert",
      type: "string",
    },
    icon: {
      description: "Optional icon element displayed before the text",
      type: "React.ReactNode",
    },
    canClosed: {
      description: "If true, the alert is dismissible with a close button",
      type: "boolean",
      default: "false",
    },
    className: {
      description: "Additional class names for the main alert container",
      type: "string",
    },
    children: {
      description:
        "Optional custom content to replace the default message. Overrides the `message` prop if provided.",
      type: "React.ReactNode",
    },
    open: {
      description: "Controlled open state. Determines if the alert is visible",
      type: "boolean",
    },
    onOpenChange: {
      description: "Function to control the open state from a parent component",
      type: "(open: boolean) => void",
    },
  }}
/>

### AlertRoot

<TypeTable
  type={{
    variant: {
      description: "The visual style of the alert",
      type: `"default" | "destructive" | "warning" | "success" | "info"`,
      default: `"default"`,
    },
    className: {
      description: "Additional class names for styling",
      type: "string",
    },
  }}
/>

### AlertTitle

<TypeTable
  type={{
    className: {
      description: "Additional class names for styling the title",
      type: "string",
    },
    children: {
      description: "Title content (typically text or heading)",
      type: "React.ReactNode",
    },
  }}
/>

### AlertDescription

<TypeTable
  type={{
    className: {
      description: "Additional class names for styling the description",
      type: "string",
    },
    children: {
      description: "Description content (typically text or elements)",
      type: "React.ReactNode",
    },
  }}
/>
