---
title: Separator
description: A simple line element that visually breaks up content, helping users quickly scan and differentiate sections.
component: true
doc: Menu#separator
---

## Example

<Playground comp="separator/basic" />

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
  <Tab value="Manual">
    Install following dependencies:

    ```package-install
    npm install react-aria-components tw-animate-css tailwindcss-react-aria-components class-variance-authority lucide-react clsx tailwind-merge motion
    ```

    Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

    <ComponentSource comp="lib/utils" isReact={false} />

    Copy and paste the following code into your project.

    <ComponentSource comp="components/ui/core/separator" />

  </Tab>
  <Tab value="CLI">
    ```package-install
    npx @libravelui@latest add separator
    ```
  </Tab>
</Tabs>

## Anatomy

```tsx
import { Separator } from "@/components/ui/core/separator";
```

```tsx
<Separator orientation="horizontal" />
<Separator orientation="vertical" />
```

## Another Example

### With Text

A separator with text placed in the middle, visually dividing content while keeping the label visible and centered within the separator.

<Playground comp="separator/with-text" />

## Props

<AutoTypeTable comp="@/components/ui/core/separator" />
