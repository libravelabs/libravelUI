---
title: Radar Chart
description: A radar chart compares multiple values across the same set of dimensions, making it easy to spot balanced and uneven profiles.
component: true
doc: RadarChart
---

## Example

<Playground path="components/examples/core/charts/radar-chart/basic.tsx" />

## Features

- Good for multi-metric comparison.
- Works well for skill, benchmark, and profile views.
- Uses the shared LibravelUI palette and tooltip.
- Supports custom angle and radius axes.
- Keeps series styling lightweight and consistent.

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
  <Tab value="Manual">
    Copy the shared chart utilities and the chart component you need into your project.

    <ComponentSource comp="components/ui/core/charts/chart-shell" />

    <ComponentSource comp="components/ui/core/charts/chart-tooltip" />

    <ComponentSource comp="components/ui/core/charts/radar-chart" />
  </Tab>
  <Tab value="CLI">
    ```package-install
    npx libravelui@latest add radar-chart
    ```
  </Tab>
</Tabs>

## Anatomy

```tsx
import { RadarChart } from "@/components/ui/core/charts/radar-chart";
```

```tsx
<RadarChart
  data={data}
  dataKey="subject"
  config={{
    desktop: { label: "Desktop" },
    mobile: { label: "Mobile" },
  }}
/>
```

## Props

<AutoTypeTable comp="@/components/ui/core/charts/radar-chart" />
