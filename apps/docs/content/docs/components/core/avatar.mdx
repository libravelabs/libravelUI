---
title: Avatar
description: An image element used to represent a user or entity, typically displayed as a circular or square thumbnail. Can fall back to initials or an icon when no image is provided.
component: true
---

## Basic

<Playground comp="avatar/basic" />

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
  <Tab value="Manual">
    <RequiredBlock components="field" />
    Install following dependencies:

    ```package-install
    npm install tw-animate-css clsx tailwind-merge class-variance-authority
    ```

    Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

    <ComponentSource comp="lib/utils" isReact={false} />

    Copy and paste the following code into your project.

    <ComponentSource comp="components/ui/core/avatar" />

  </Tab>
  <Tab value="CLI">
    ```package-install
    npx libravelui@latest add avatar
    ```
  </Tab>
</Tabs>

## Anatomy

```tsx
import { Avatar } from "@/components/ui/core/avatar";
```

```tsx
<Avatar src="avatar-src" alt="avatar-alt" />
```

## Examples

### Fallback

Displays an avatar using user initials when no profile image is available. This ensures consistent styling and fallback behavior while still giving a personalized touch with capitalized initials.

<Playground comp="avatar/initial" />

### Shapes

Showcases different avatar shapes: **circle** and **square**. The shape can be controlled via the `shape` prop, allowing you to match the avatar’s appearance with your app’s design language.

<Playground comp="avatar/shape" />

### Sizes

Demonstrates the avatar in various sizes, from **extra small (xs)** to **extra large (xl)**. The `size` prop offers flexibility for using the avatar in different UI contexts—such as user lists, comments, or main profile views.

<Playground comp="avatar/sizes" />

### Avatar Group

Displays a group of avatars representing multiple users. Each avatar can show either a profile image or fallback initials if the image is not available. Avatar groups are commonly used in collaborative interfaces, team listings, or messaging apps.

<Playground comp="avatar/group" />

## Props

<AutoTypeTable comp="@/components/ui/avatar" />
