---
title: Badge
description: A small label used to display statuses, counts, or notifications.
component: true
---

## Example

<Playground path="components/examples/core/badge/basic.tsx" />

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
  <Tab value="Manual">
    <RequiredBlock components="field" />
    Install following dependencies:

    ```package-install
    npm install class-variance-authority clsx tailwind-merge @radix-ui/react-slot
    ```

    Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

    <ComponentSource comp="lib/utils" isReact={false} />

    Copy and paste the following code into your project.

    <ComponentSource comp="components/ui/core/badge" />

  </Tab>
  <Tab value="CLI">
    ```package-install
    npx @libravelui@latest add badge
    ```
  </Tab>
</Tabs>

## Anatomy

```tsx
import { Badge } from "@/components/ui/core/badge";
```

```tsx
<Badge tone="default">Default</Badge>
<Badge tone="secondary">Secondary</Badge>
<Badge tone="destructive">Destructive</Badge>
<Badge tone="outline">Outline</Badge>
```

## Another Example

### As Child

Displays different types of badges used to indicate status or categories with flexible styling that can be applied directly to other elements without adding extra DOM nodes.

<Playground path="components/examples/core/badge/as-child.tsx" />

## Props

<AutoTypeTable comp="@/components/ui/core/badge" />
