---
title: Accordion
description: An Accordion is a grouping of related disclosures. It supports both single and multiple expanded items.
component: true
doc:
  title: Disclosure Group
  url: https://react-spectrum.adobe.com/react-aria/DisclosureGroup.html
---

## Basic

<Playground comp="accordion/basic" />

## Features

Disclosure groups can be built by combining multiple disclosures built in HTML with the `<details>` and `<summary>` HTML elements, but this can be difficult to style, especially when adding adjacent interactive elements, like buttons, to the disclosure's heading. DisclosureGroup helps achieve accessible disclosures implemented with the correct ARIA pattern while making custom styling easy.

- **Accessible** - Disclosure group is exposed to assistive technology via ARIA. Uses hidden="until-found" in supported browsers, enabling find-in-page search support and improved search engine visibility for collapsed content.
- **Styleable** - Disclosures include builtin states for styling such as keyboard focus, disabled, and expanded states.

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
  <Tab value="Manual">
    <RequiredBlock components="field" />
    Install following dependencies:

    ```package-install
    npm install react-aria-components tw-animate-css tailwindcss-react-aria-components clsx tailwind-merge motion
    ```

    Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

    <ComponentSource comp="lib/utils" isReact={false} />

    Copy and paste the following code into your project.

    <ComponentSource comp="components/ui/accordion" />

  </Tab>
  <Tab value="CLI">
    ```package-install
    npx libravelui@latest add accordion
    ```
  </Tab>
</Tabs>

## Anatomy

```tsx
import {
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
} from "@/components/ui/accordion";
```

```tsx
<Accordion>
  <AccordionItem>
    <AccordionTrigger>Trigger goes</AccordionTrigger>
    <AccordionContent>The content goes here</AccordionContent>
  </AccordionItem>
</Accordion>
```

## Example

### Variants

The Accordion component offers visual variants like `default`, `ghost`, `outline`, `solid`, `muted`, and `separated` for different use cases.

<Playground comp="accordion/variants" />

## Props

<TypeTable
  type={{
    variant: {
      description:
        'Visual style of the accordion. Options: `"default"`, `"ghost"`, `"outline"`, `"solid"`, `"muted"`, `"separated"`.',
      type: `"default" | "ghost" | "outline" | "solid" | "muted" | "separated"`,
      required: false,
    },
    size: {
      description: 'Size of the accordion. Options: `"sm"`, `"md"`, `"lg"`, `"full"`.',
      type: `"sm" | "md" | "lg" | "full"`,
      required: false,
    },
    className: {
      description: "Additional class names to apply to the accordion container.",
      type: "string",
      required: false,
    },
    ref: {
      description: "A React ref to access the accordion container DOM element.",
      type: "React.RefObject<HTMLDivElement>",
      required: false,
    },
    multiple: {
      description: "Allow multiple items to be expanded at the same time.",
      type: "boolean",
      required: false,
    },
    children: {
      description:
        "Accordion items or a render function that receives the expanded item keys.",
      type: "React.ReactNode | ((values: Key[]) => React.ReactNode)",
      required: true,
    },

    allowsMultipleExpanded: {
      description: "Whether multiple items can be expanded at the same time.",
      type: "boolean",
      required: false,
    },
    isDisabled: {
      description: "Whether all items are disabled.",
      type: "boolean",
      required: false,
    },
    expandedKeys: {
      description: "The currently expanded keys in the group (controlled).",
      type: "Iterable<Key>",
      required: false,
    },
    defaultExpandedKeys: {
      description: "The initial expanded keys in the group (uncontrolled).",
      type: "Iterable<Key>",
      required: false,
    },
    style: {
      description:
        "The inline style for the element. A function may be provided to compute the style based on component state.",
      type: "React.CSSProperties | ((props: DisclosureGroupRenderProps) => React.CSSProperties)",
      required: false,
    },

    onExpandedChange: {
      description: "Handler that is called when items are expanded or collapsed.",
      type: "(keys: Set<Key>) => any",
      required: false
    }

}}
/>
