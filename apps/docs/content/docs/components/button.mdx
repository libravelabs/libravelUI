---
title: Button
description: A button lets users perform an action or trigger an event using mouse, touch, or keyboard input. It's a key interactive element in most user interfaces.
component: true
doc: https://react-spectrum.adobe.com/react-aria/Button.html
---

import { RequiredBlock } from "@/components/required-block";
import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { BasicButton, ButtonVariants, ButtonSizes, ButtonRadius, WithIconButton, LoadingButton, DisabledButton } from '@/components/docs/button'

## Basic
<BasicButton />

## Features
- **Styleable** – Hover, press, and keyboard focus states are provided for easy styling. These states only apply when interacting with an appropriate input device, unlike CSS pseudo classes.
- **Accessible** – Uses a native `<button>` element under the hood, with support for the Space and Enter keys.
- **Cross-browser** – Mouse, touch, keyboard, and focus interactions are normalized to ensure consistency across browsers and devices.

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
<Tab value="Manual">

<RequiredBlock components="loader" />

Install following dependencies:

```package-install
npm install react-aria-components tw-animate-css tailwindcss-react-aria-components class-variance-authority clsx tailwind-merge
```

Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

```ts ts2js title="lib/utils.ts"
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Copy and paste the following code into your project.

```tsx ts2js title="components/ui/button"
"use client";

import { cva, type VariantProps } from "class-variance-authority";
import {
  Button as ButtonPrimitive,
  type ButtonProps as ButtonPrimitiveProps,
  composeRenderProps,
} from "react-aria-components";
import { cn } from "@/lib/utils";
import { Loader } from "@/components/ui/loader";

const buttonVariants = cva(
  [
    "inline-flex items-center justify-center gap-2 whitespace-nowrap text-sm font-medium transition-all disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none cursor-pointer",
    "focus:outline-0 focus-visible:outline focus-visible:outline-offset-2 focus-visible:ring-2 focus-visible:ring-offset-3 focus-visible:ring-offset-background",
  ],
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground hover:opacity-70",
        destructive: "bg-destructive text-white hover:opacity-70",
        outline: [
          "border bg-background hover:opacity-70",
          "inset-ring inset-ring-input focus:inset-ring-ring/70 focus:ring-3 focus:ring-ring/20",
        ],
        secondary: "bg-secondary text-secondary-foreground hover:opacity-70",
        ghost: "hover:bg-foreground/10",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default: "h-9 px-4 py-2 has-[>svg]:px-3",
        xs: "h-6 gap-0.5 px-2 has-[>svg]:px-1.5",
        sm: "h-8 gap-1.5 px-3 has-[>svg]:px-2.5",
        lg: "h-10 px-6 has-[>svg]:px-4",
        icon: "size-9",
      },
      radius: {
        none: "rounded-none",
        sm: "rounded-sm",
        md: "rounded-md",
        lg: "rounded-lg",
        full: "rounded-full",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
      radius: "md",
    },
  }
);

type ButtonProps = ButtonPrimitiveProps &
  VariantProps<typeof buttonVariants> & {
    ref?: React.Ref<HTMLButtonElement>;
    className?: string;
    isLoading?: boolean;
    loader?: React.ReactNode;
    children?: React.ReactNode;
    plain?: boolean;
  };

function Button({
  className,
  variant,
  size,
  radius,
  isLoading = false,
  plain = false,
  loader,
  children,
  ...props
}: ButtonProps) {
  if (plain) {
    return (
      <ButtonPrimitive>
        {(values) =>
          typeof children === "function" ? children(values) : children
        }
      </ButtonPrimitive>
    );
  }

  return (
    <ButtonPrimitive
      {...props}
      isDisabled={props.isDisabled || isLoading}
      className={composeRenderProps(className, (className) =>
        cn(buttonVariants({ variant, size, radius, className }), className)
      )}
    >
      {(values) => {
        if (isLoading) {
          return size === "icon" ? (
            loader ?? <Loader className="text-inherit" />
          ) : (
            <>
              {loader ?? <Loader className="text-inherit" />}
              {children}
            </>
          );
        }
        return typeof children === "function" ? children(values) : children;
      }}
    </ButtonPrimitive>
  );
}

export { Button, buttonVariants };
export type { ButtonProps };
```

</Tab>
<Tab value="CLI">

```package-install
npx libravelui@latest add button
```

</Tab>
</Tabs>

## Anatomy

```tsx
import { Button } from "@/components/ui/button";
```

```tsx
<Button variant="default">Default</Button>
<Button variant="secondary">Secondary</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="link">Link</Button>
<Button variant="destructive">Destructive</Button>
```

## Examples

### Variants
Different visual styles for buttons, such as `primary`, `secondary`, `outline`, `ghost`, `link`, and `destructive`, useful for establishing emphasis and hierarchy in the UI.
<ButtonVariants />

### Sizes
Adjusts the button’s dimensions, providing options like `xs`, `sm`, `default`, `lg`, and `icon`, to fit different UI contexts and layouts.
<ButtonSizes />

### Radius (rounded)
Controls the border-radius of the button to achieve styles ranging from sharp corners to fully rounded pills.
<ButtonRadius />

### With Icon
Combines an icon with a text label or renders an icon-only button to enhance clarity or visual appeal.
<WithIconButton />

### Loading State
Displays a spinner or progress indicator inside the button to communicate ongoing activity, such as submitting a form.
<LoadingButton />

### Disabled
Prevents user interaction with the button while visually indicating that the action is unavailable or inactive.
<DisabledButton />

## Props

<TypeTable
  type={{
    variant: {
      description: "Visual style variant of the button.",
      type: "'default' | 'destructive' | 'outline' | 'secondary' | 'ghost' | 'link'",
      required: false,
    },
    size: {
      description: "Size of the button.",
      type: "'default' | 'xs' | 'sm' | 'lg' | 'icon'",
      required: false,
    },
    radius: {
      description: "Border radius of the button.",
      type: "'none' | 'sm' | 'md' | 'lg' | 'full'",
      required: false,
    },
    className: {
      description: "Additional class names for the component.",
      type: "string",
      required: false,
    },
    isDisabled: {
      description: "Whether the button is disabled. Overrides disabled if provided.",
      type: "boolean",
      required: false,
    },
    isLoading: {
      description: "Show loading state and disable button interaction.",
      type: "boolean",
      required: false,
    },
    loader: {
      description: "Custom loader component to display during loading state.",
      type: "React.ReactNode",
      required: false,
    },
    ref: {
      description: "Ref forwarded to the native button element.",
      type: "React.Ref<HTMLButtonElement>",
      required: false,
    },
    children: {
      description: "Content of the button, can be ReactNode or render function.",
      type: "React.ReactNode | ((state: any) => React.ReactNode)",
      required: true,
    },
    isPending: {
      description: "Whether the button is in a pending state; disables press and hover but keeps focus.",
      type: "boolean",
      required: false,
    },
    autoFocus: {
      description: "Whether the button should auto focus on render.",
      type: "boolean",
      required: false,
    },
    type: {
      description: "Button behavior in a form.",
      type: "'button' | 'submit' | 'reset'",
      required: false,
    },
    form: {
      description: "ID of the form the button is associated with.",
      type: "string",
      required: false,
    },
    formAction: {
      description: "URL to process the form data when submitted.",
      type: "string",
      required: false,
    },
    formEncType: {
      description: "Encoding type for form submission.",
      type: "string",
      required: false,
    },
    formMethod: {
      description: "HTTP method for form submission.",
      type: "string",
      required: false,
    },
    formNoValidate: {
      description: "Disables form validation on submit.",
      type: "boolean",
      required: false,
    },
    formTarget: {
      description: "Specifies where to display the response after submitting the form.",
      type: "string",
      required: false,
    },
    name: {
      description: "Name for the button used in form submission.",
      type: "string",
      required: false,
    },
    value: {
      description: "Value submitted with the button's name.",
      type: "string",
      required: false,
    },
    style: {
      description: "Inline style or a function returning styles based on button state.",
      type: "React.CSSProperties | ((state: any) => React.CSSProperties)",
      required: false,
    },
  }}
/>
