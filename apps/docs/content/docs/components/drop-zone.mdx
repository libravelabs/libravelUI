---
title: Drop Zone
description: A drop zone is an area into which one or multiple objects can be dragged and dropped.
component: true
doc: https://react-spectrum.adobe.com/react-aria/DropZone.html
---

import { RequiredBlock } from "@/components/required-block";
import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { BasicDropZone, DropZoneVariants } from '@/components/docs/drop-zone'

## Basic
<BasicDropZone />

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
<Tab value="Manual">
<RequiredBlock components="field" />

Install following dependencies:

```package-install
npm install react-aria-components tw-animate-css tailwindcss-react-aria-components class-variance-authority clsx tailwind-merge
```

Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

```ts ts2js title="lib/utils.ts"
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Copy and paste the following code into your project.

```tsx ts2js title="components/ui/drop-zone"
CODE HERE
```

</Tab>
<Tab value="CLI">

```package-install
npx libravelui@latest add drop-zone
```

</Tab>
</Tabs>

## Anatomy

```tsx
import { DropZone } from "@/components/ui/drop-zone";
```

```tsx
<DropZone>Hit us with your best shot!</DropZone>
```

## Example
### Variants
Customizable styles and sizes for the DropZone component. Great for adapting to different layouts and visual themes.
<DropZoneVariants />

## Props

<TypeTable
  type={{
    className: {
      description: "Additional class names for the component",
      type: "string",
      required: false,
    },
    variant: {
      description: "Visual style of the drop zone",
      type: `"default" | "dashed" | "ghost"`,
      required: false,
    },
    size: {
      description: "Controls padding, height, and max width",
      type:
        `"xs" | "sm" | "md" | "lg" | "xl" | "2xl" | "3xl" | "4xl" | "5xl" | "6xl" | "7xl" | "full"`,
      required: false,
    },
    isDropTarget: {
      description: "Whether the drop zone is currently a drop target",
      type: "boolean",
      required: false,
    },
    label: {
      description: "Optional label displayed above the drop zone",
      type: "string",
      required: false,
    },
    description: {
      description: "Optional helper text displayed below the drop zone",
      type: "string",
      required: false,
    },
  }}
/>
