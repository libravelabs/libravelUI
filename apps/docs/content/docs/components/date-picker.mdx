---
title: Date Picker
description: A compound component built on top of a date field and calendar popover, providing accessible date selection through typing or visual interaction.
component: true
doc: https://react-spectrum.adobe.com/react-aria/DatePicker.html
---

import { RequiredBlock } from "@/components/required-block";
import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { BasicDatePicker, DateTimePicker } from '@/components/docs/date-picker'

## Basic
<BasicDatePicker />

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
<Tab value="Manual">

<RequiredBlock components={["calendar", "date-field", "field", "popover"]} />

Install following dependencies:

```package-install
npm install react-aria-components tw-animate-css tailwindcss-react-aria-components @internationalized/date class-variance-authority lucide-react clsx tailwind-merge
```

Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

```ts ts2js title="lib/utils.ts"
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Copy and paste the following code into your project.

```tsx ts2js title="components/ui/date-picker"
"use client";

import type { DateDuration } from "@internationalized/date";
import {
  DatePicker as DatePickerPrimitive,
  type DatePickerProps as DatePickerPrimitiveProps,
  type DateValue,
  type PopoverProps,
} from "react-aria-components";
import { cn } from "@/lib/utils";
import { Calendar } from "@/components/ui/calendar";
import { DateInput } from "@/components/ui/date-field";
import {
  Description,
  FieldError,
  FieldGroup,
  type FieldProps,
  Label,
} from "@/components/ui/field";
import { PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { CalendarIcon } from "lucide-react";

interface DatePickerProps<T extends DateValue>
  extends DatePickerPrimitiveProps<T>,
    Pick<PopoverProps, "placement">,
    Omit<FieldProps, "placeholder"> {
  range?: boolean;
  visibleDuration?: DateDuration;
  pageBehavior?: "visible" | "single";
}

function DatePicker<T extends DateValue>({
  label,
  className,
  description,
  error,
  placement,
  ...props
}: DatePickerProps<T>) {
  return (
    <DatePickerPrimitive
      aria-label={props["aria-label"] ?? "date-picker"}
      className={cn("group flex flex-col gap-y-1", className)}
      {...props}
    >
      {label && <Label>{label}</Label>}
      <FieldGroup className="min-w-72 inset-ring inset-ring-input outline-hidden focus:inset-ring-ring/70 focus:ring-3 focus:ring-ring/20 group-open:inset-ring-ring/70 group-open:ring-3 group-open:ring-ring/20">
        <DateInput className="w-full" />
        <PopoverTrigger
          asPrimitive
          className="group-disabled:opacity-20 group-disabled:cursor-not-allowed"
        >
          <CalendarIcon className="text-muted-foreground" />
        </PopoverTrigger>
      </FieldGroup>
      {description && <Description>{description}</Description>}
      <FieldError>{error}</FieldError>

      <PopoverContent
        withArrow={false}
        placement={placement}
        className="p-2 flex min-w-auto max-w-none snap-x justify-center"
        {...props}
      >
        <Calendar />
      </PopoverContent>
    </DatePickerPrimitive>
  );
}
export type { DatePickerProps };
export { DatePicker };
```

</Tab>
<Tab value="CLI">

```package-install
npx libravelui@latest add date-picker
```

</Tab>
</Tabs>

## Anatomy

```tsx
import { DatePicker } from "@/components/ui/date-picker";
```

```tsx
<DatePicker />
```

## Example
### Date Time Picker
A simple and accessible way to select an exact date and time.
<DateTimePicker />

## Props

<TypeTable
  type={{
    label: {
      description: "Label text displayed above the date picker field",
      type: "string",
      required: false,
    },
    description: {
      description: "Optional helper text displayed below the picker",
      type: "string",
      required: false,
    },
    error: {
      description:
        "Error message or function returning a validation-specific error message",
      type: "string | ((validation: ValidationResult) => string)",
      required: false,
    },
    className: {
      description: "Additional class names for the wrapper element",
      type: "string",
      required: false,
    },
    range: {
      description: "Enables date range selection instead of a single date",
      type: "boolean",
      required: false,
    },
    visibleDuration: {
      description:
        "Defines how many months are visible in the calendar popover",
      type: "DateDuration",
      required: false,
    },
    pageBehavior: {
      description:
        "Controls how calendar pages change when navigating (either 'visible' or 'single')",
      type: `"visible" | "single"`,
      required: false,
    },
    placement: {
      description:
        "Controls the placement of the calendar popover relative to the input field",
      type: "PopoverProps['placement']",
      required: false,
    },
    isDisabled: {
      description: "Disables the picker so it cannot be interacted with",
      type: "boolean",
      required: false,
    },
    isReadonly: {
      description: "Makes the picker read-only without allowing edits",
      type: "boolean",
      required: false,
    },
  }}
/>
