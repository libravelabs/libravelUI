---
title: Comparator
description:
component: true
---

## Basic

<Playground comp="motion/comparator/basic" />

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
  <Tab value="Manual">
    Install following dependencies:

    ```package-install
    npm install react-aria-components tw-animate-css tailwindcss-react-aria-components class-variance-authority lucide-react clsx tailwind-merge motion
    ```

    Add `cn` helper using `clsx` and `twMerge` for conditional class merging in Tailwind components.

    <ComponentSource comp="lib/utils" isReact={false} />

    Copy and paste the following code into your project.

    <ComponentSource comp="components/ui/comparator" />

  </Tab>
  <Tab value="CLI">
    ```package-install
    npx libravelui@latest add comparator
    ```
  </Tab>
</Tabs>

## Anatomy

```tsx
import {
  Comparator,
  ComparatorPane,
  ComparatorSlider,
} from "@/components/ui/motion/comparator";
```

```tsx
<Comparator>
  <ComparatorPane position="left">{/* Some Content */}</ComparatorPane>

  <ComparatorPane position="right">{/* Some Content */}</ComparatorPane>

  <ComparatorSlider />
</Comparator>
```

## Examples

### Not Image

Applies motion effects to elements that aren’t images, showing how animations can enhance other types of content.

<Playground comp="motion/comparator/not-image" />

### Enable Hover

Enables motion effects that respond to hover interactions. Elements smoothly animate when hovered, adding interactivity and visual feedback.

<Playground comp="motion/comparator/enable-hover" />

### Spring Options

Provides controls to fine-tune spring animations, allowing you to adjust stiffness, damping, and mass for precise motion behavior.

<Playground comp="motion/comparator/spring-options" />

### Custom Slider

A slider with customizable styling and motion behavior, offering flexibility in design and interaction.

<Playground comp="motion/comparator/custom-slider" />

## Props

<AutoTypeTable comp="@/components/ui/motion/comparator" />
